module.exports = {
  plugins: {
    // Tailwind CSS v4+ uses a separate PostCSS plugin package.
    // Use '@tailwindcss/postcss' so PostCSS can load Tailwind correctly.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
